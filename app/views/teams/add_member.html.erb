<%= form_for :team_member, url: submit_member_team_path do |f| %>

  <div class="field">
  <p> Team:- <%=Team.find(params[:id]).name%></p>
    <%#= f.label :'Team.find(params[:id].name)' %>
    <%= f.hidden_field :team_id, :value => params[:id] %>
    <%#= f.select(:team_id, @teams.collect {|t| [t.name, t.id]},:prompt => 'Select team') %>
  </div>

  <div class="field">
    <%= f.label :member %>
    <select name="member">
        
        <%@users.each do |user|%>
        <%unless user.team.present?%>
        <option value="<%=user.id%>"><%=user.email%></option>
       <%end%>
        <%end%>
    </select>
  </div>
  
  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Back', teams_path %>
